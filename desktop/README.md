# BADER Desktop (Tauri + React)

Desktop application with offline-first SQLite database.

## Development

```bash
# Install dependencies
npm install

# Run in dev mode
npm run tauri dev

# Build for production
npm run tauri build
```

## Features

- Offline-first with SQLite
- Hybrid sync with backend
- Role-based permissions
- License validation
