import React from 'react';

interface GelirFormProps {
  // TODO: Define props
}

export const GelirForm: React.FC<GelirFormProps> = (props) => {
  return (
    <div>
      <h2>GelirForm</h2>
      {/* TODO: Implement component */}
    </div>
  );
};
